@model List<LeaveVM>
<div class="card">
    <div class="card-body">
        <h4 class="card-title">Leave Requests</h4>
        <div class="row">
            <div class="col-12">
                <div class="table-responsive">
                    <form>
                        <table id="order-listing" class="table">
                            <thead>
                                <tr>
                                    <th>Start Date</th>
                                    <th>End Date</th>
                                    <th>Personal</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.StartDate</td>
                                        <td>@item.EndDate</td>
                                        <td>@item.PersonalFullName</td>
                                        <td>
                                            <button class="btn btn-outline-primary" asp-controller="Leave" asp-action="LeaveRequestDetails" asp-route-id="@item.Id">Details</button>
                                            <button class="btn btn-outline-danger" asp-controller="Leave" asp-action="Delete" asp-route-id="@item.Id">Refuse</button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>