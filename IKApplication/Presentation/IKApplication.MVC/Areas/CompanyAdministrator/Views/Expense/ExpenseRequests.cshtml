@model List<ExpenseVM>
<div class="card">
    <div class="card-body">
        <h4 class="card-title">Expense Requests</h4>
        <div class="row">
            <div class="col-12">
                <div class="table-responsive">
                    <form>
                        <table id="order-listing" class="table">
                            <thead>
                                <tr>
                                    <th>Short Description</th>
                                    <th>Amount</th>
                                    <th>Type</th>
                                    <th>Expense Date</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.ShortDescription</td>
                                        <td>@item.Amount</td>
                                        <td>@item.Type</td>
                                        <td>@item.ExpenseDate</td>
                                        <td>
                                            <button class="btn btn-outline-primary" asp-controller="Expense" asp-action="ExpenseRequestDetails" asp-route-id="@item.Id">Details</button>
                                            <button class="btn btn-outline-danger" asp-controller="Expense" asp-action="Delete" asp-route-id="@item.Id">Refuse</button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>